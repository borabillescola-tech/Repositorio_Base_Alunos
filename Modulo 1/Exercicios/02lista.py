# Dada a lista de numeros = [1, 2. 3], use .extend()
# para adicionar os elementos de outra lista mais_numeros = [4, 5, 6]
# Mostre a lista no final

# Resolvido

numeros = [1, 2, 3] # lista inicial
print(numeros) # mostramos a lista inicial

mais_numeros = [4, 5, 6] # nova lista
# recebe uma lista
print(mais_numeros) # mostramos a nova lista
